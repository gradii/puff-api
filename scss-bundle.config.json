{
  "*1": "scss-bundle requires a config file in the Bazel execroot.",
  "*2": "The config is used in the src/triangle:theming_bundle target",
  "bundlerOptions": {
    "logLevel": "error",
    "dedupeGlobs": [
      "./src/**/*.scss"
    ]
  }
}
